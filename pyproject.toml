[build-system]
requires = ["setuptools>=68,<85"]
build-backend = "setuptools.build_meta"

[project]
name = "dns-security-checker"
version = "0.3.0"
description = "Inspect defensive DNS records for one authorized public hostname."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Oscar Opemba" }]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Information Technology",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]

[project.urls]
Repository = "https://github.com/Oscar-Opemba/dns-security-checker"
Issues = "https://github.com/Oscar-Opemba/dns-security-checker/issues"

[project.scripts]
dns-security-checker = "app:main"

[tool.setuptools]
py-modules = ["app", "common", "security_utils"]
